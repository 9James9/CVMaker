import React from "react";

export function WorkHistoryDisplay({ history }) {
  return (
    <div>
      <h2>Hello Workhistorydisplay</h2>
      {/* {Object.keys(history.jobs).map((key,value) => {
          return console.log(key,value)
      })} */}
    </div>
  );
}
